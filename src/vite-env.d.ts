/// <reference types="vite/client" />

// Aucune variable d'environnement, client ou serveur.
//
// La démonstration est entièrement statique : pas d'API, donc pas de jeton,
// pas de proxy, pas de `.env`. Un `git clone` suivi de `npm ci` suffit à
// construire le site.
